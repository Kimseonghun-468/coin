apiVersion: v1
kind: Service
metadata:
  name: redis
spec:
  selector:
    app: redis
  ports:
    - protocol: TCP
      port: 6379  # 외부에서 접근할 포트
      targetPort: 6379  # 컨테이너 내부 포트
  type: ClusterIP  # 기본값: ClusterIP. 외부에서 접근할 필요 없으면 이걸 사용
